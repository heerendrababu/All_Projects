# Enable Embedded Artemis Broker
spring.artemis.embedded.enabled=true

# Disable persistence (in-memory mode)
spring.artemis.embedded.persistent=false

# Default Artemis queue name
spring.artemis.embedded.queues=test-queue

# Optional: Set the time-to-live for messages
spring.artemis.time-to-live=0
